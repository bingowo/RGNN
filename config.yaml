model:
  n_hidden: 1024
  n_layers: 4
  n_heads: 1

training:
  lr: 1e-2
  weight_decay: 5e-5
  n_epochs: 1000
  patience: ~
  criterion: ~

dataset:
  feat_norm: true
  sparse: true

analysis:
  flag: false
  project: gsl
  save_graph: false